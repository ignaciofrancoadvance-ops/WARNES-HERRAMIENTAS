/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Agregá acá los dominios remotos de imágenes si usás un CDN.
    remotePatterns: [],
  },
};

export default nextConfig;
